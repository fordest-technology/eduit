export const theme = {
  colors: {
    primary: {
      default: "var(--primary-color, #3b82f6)", // blue-500 as fallback
      light: "var(--primary-color-light, #60a5fa)", // blue-400 as fallback
      dark: "var(--primary-color-dark, #2563eb)", // blue-600 as fallback
    },
    secondary: {
      default: "var(--secondary-color, #1f2937)", // gray-800 as fallback
      light: "var(--secondary-color-light, #374151)", // gray-700 as fallback
      dark: "var(--secondary-color-dark, #111827)", // gray-900 as fallback
    },
  },
};
